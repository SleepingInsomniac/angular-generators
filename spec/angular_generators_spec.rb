require 'spec_helper'

describe AngularGenerators do
  it 'has a version number' do
    expect(AngularGenerators::VERSION).not_to be nil
  end

  it 'does something useful' do
    expect(false).to eq(true)
  end
end
